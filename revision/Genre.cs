namespace revision
{
    // used for casting a band object to the appropriate type from the BandFactory,
    // when a new band is made for a json object

    public enum Genre
    {
        Rock,
        Pop,
        Indie
    }
}
